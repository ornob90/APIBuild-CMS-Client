"use server"

export const getUserToken = async() => {
    try {
        // const response = await 
    } catch {
        return []
    }
}

export const generateToken = async () => {
    try {
        
    } catch {
        return ""
    }
}