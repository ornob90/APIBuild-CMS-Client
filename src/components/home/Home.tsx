import SectionContainer from "../shared/SectionContainer";
import Dashboard from "./Dashboard";

const Home = () => {
  return (
    <SectionContainer>
       <Dashboard />
    </SectionContainer>
  );
};

export default Home;
